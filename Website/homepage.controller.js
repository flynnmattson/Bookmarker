app.controller('homepageCtrl', '$scope', 'Auth', 'ref',
    function($scope, Auth, ref) {

      $scope.downloadExtension = function()
      {
          window.location.href = url('https://chrome.google.com/webstore/category/apps';
      }

  }
]);
